<Window x:Class="GuildTracker.Views.PriorityDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Set Priority Players" Height="500" Width="420"
        WindowStartupLocation="CenterOwner"
        Background="#1e1e2e" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Priority Players (Next Distribution)" FontSize="16" FontWeight="SemiBold"
                   Foreground="#cba6f7" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="1" Text="These players will be ⭐ priority. All current priority flags will be replaced."
                   Foreground="#a6adc8" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,12"/>

        <!-- Priority list -->
        <Border Grid.Row="2" Background="#313244" CornerRadius="6" Padding="4">
            <ListBox x:Name="PriorityList" ItemsSource="{Binding PriorityPlayers}"
                     Background="Transparent" BorderThickness="0"
                     Foreground="#cdd6f4" SelectionMode="Extended">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Padding="4,3" Foreground="#cdd6f4"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- Add from all members -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,0">
            <ComboBox x:Name="MemberPicker" Width="220" Margin="0,0,8,0"
                      ItemsSource="{Binding AllMembers}"
                      SelectedItem="{Binding SelectedMemberToAdd}"/>
            <Button Content="+ Add" Click="Add_Click"
                    Background="#89b4fa" Foreground="#1e1e2e" FontWeight="SemiBold"
                    Padding="12,6" BorderThickness="0" Cursor="Hand" Margin="0,0,6,0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Content="✕ Remove" Click="Remove_Click"
                    Background="#f38ba8" Foreground="#1e1e2e" FontWeight="SemiBold"
                    Padding="12,6" BorderThickness="0" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>

        <!-- OK / Cancel -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Cancel" Click="Cancel_Click"
                    Background="#45475a" Foreground="#cdd6f4" FontWeight="SemiBold"
                    Padding="16,7" BorderThickness="0" Cursor="Hand" Margin="0,0,8,0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Content="✓ OK" Click="Ok_Click"
                    Background="#a6e3a1" Foreground="#1e1e2e" FontWeight="SemiBold"
                    Padding="16,7" BorderThickness="0" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Grid>
</Window>
